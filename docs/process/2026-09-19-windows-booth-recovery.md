# Sway Windows booth recovery continuation

Objective: Repair the existing no-install Windows connector used by performers, not add another player adapter. Audio remains in VirtualDJ; operator actions remain deliberate.
Base branch/commit: `implement/direct-music-control-20260916`, PR249, `55fc8b6268db747116f3241351381c58f9a785ad`.
Current source: The commit containing this checkpoint. Prior direct-music reconnect validation at `58a6fc0` remains historical proof for its own files.
Verified completed work: The generated PowerShell rejects unreadable player state and accepts only exact `true` execution acknowledgements. It saves an unknown outcome before dispatch, retains bridge identity and pending completions across restart, preserves uncertain outcomes during bounded-history cleanup, and refuses corrupt/foreign-room ledgers instead of resetting them.
Recovery behavior: Failed completion delivery blocks new claims. An unknown command stops the remaining batch. The performer must inspect the original deck and explicitly confirm after a readable state check before new commands can proceed. The old command is never replayed. Valid old-format records migrate conservatively; old failures remain unknown.
Durability: Write-through temporary files are flushed and replaced on the same volume. PowerShell's actual null-string value is used for the .NET replacement call. Failed persistence prevents dispatch; a stopped/crashed process leaves the conservative receipt intact.
Files changed: `src/server/windows-booth-launcher.ts`, `scripts/sway-windows-booth-launcher.test.ts`, new `scripts/sway-windows-booth-recovery.native.test.mjs`, `docs/SWAY_CONTROL_BRIDGE.md`, and this checkpoint.
Executed focused proof: The old runtime failed the new unreadable-state regression. The repaired generated functions passed 14 named native PowerShell checks. A separate two-process test abruptly exits the owned first process at dispatch, then restarts from the same ledger: exactly one synthetic dispatch, preserved unknown result and bridge identity, no replay.
Test scope: The real generated PowerShell functions execute on Windows with synthetic transport replies and owned temporary ledgers. The outer downloaded CMD startup/prompt sequence, installed VirtualDJ extension, physical device, and audible playback are not established by this proof.
Hard-gate wiring: The existing Windows launcher test now invokes native behavior on Windows and fails on nonzero exit, signal or timeout. Other platforms retain the portable source checks and explicitly report the missing native Windows proof. Existing library-helper checks and mandatory contract commands remain intact.
Changed but unverified: The full integration gate for this new Windows candidate has not yet run at this checkpoint. No release acceptance is claimed.
Evidence location: `../sway-player-acceptance-20260919/` retains native-red.log, unsuccessful intermediate attempts, windows-contract-final.log and the current execution checkpoint. No earlier evidence was overwritten.
Observed machine: No VirtualDJ/Spotify/Mixxx process was returned and no checked standard executable path existed. This was a narrow availability check, not a complete installed-software inventory.
External side effects: Only Sway source, synthetic local tests and owned temporary files. No real provider calls, playback, subscriptions, customer data, cloud resources, production migrations, merge or deployment. Historical committed receipts and earlier preserved proof copies remain unchanged. Invoked suites refresh their disposable tmp outputs; copy those outputs before the final integration run.
Next exact action: Commit and run the unchanged full Sway validation against that exact candidate, preserving prior generated proof folders; keep provider/device and release acceptance separate.
Do not repeat: The completed reconnect audit/gate unless current changes invalidate its tests. Do not touch TradeScout, JW Stone, MealScout or AutoBott. Do not launch a real player, replay uncertain commands, weaken an assertion, merge main or deploy without separate authorization.
Rollback: Revert the coordinated Windows generator/test slice. No application database change exists. Newly generated room files contain the repair; previously downloaded files do not update themselves.

Primary protocol reference: https://virtualdj.com/wiki/NetworkControlPlugin.html (query versus execute and exact execute acknowledgement).
Primary platform references: https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace and https://learn.microsoft.com/en-us/dotnet/api/system.management.automation.language.nullstring .
