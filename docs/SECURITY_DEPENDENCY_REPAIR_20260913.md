# Security dependency repair — 2026-09-13

This separate follow-up builds on recovery candidate `ff2d4663d296584824693fec1b64d12e2a5e8e0c`, tree `e1a12205427bb1fd9eeb40ea06f5541a53215970`. Its isolated deployment `dep-dajeqifqj5pc73dbl1o0` passed all 15 steps, including full contracts and the native PostgreSQL Sources journey. [Recovery receipt](https://sway-release-proof.onrender.com/source-evidence.json) is updated by later proof deployments; the commit and deployment identifiers identify that earlier result.

## Changes and reason

- Sharp 0.35.3 → 0.35.4, including prebuilt libvips packages and libheif 1.23.2. This addresses the [upstream untrusted-image advisory](https://github.com/advisories/GHSA-rgj7-g3m4-5g8c). Image behavior remains covered by actual PNG, JPEG, WebP and AVIF decode/resize checks.
- The Express/body-parser dependency tree uses qs 6.16.0 through an explicit override, addressing [the untrusted constructor denial-of-service advisory](https://github.com/advisories/GHSA-4mjr-xmp4-gh2g). Express stays on 4.22.2. Nested query and hostile-input round trips are checked.
- Browserslist 4.28.9 and baseline-browser-mapping 2.11.23 replace affected build-time packages. Browser data packages update within their existing ranges.
- Only `@esbuild-kit/core-utils` overrides its old esbuild dependency to 0.25.12. Drizzle Kit remains 0.31.10. Both loader transform APIs and actual Drizzle schema generation are checked; no database connection or migration application is used by schema generation.
- The compatible tsx esbuild patch updates to 0.28.2. Platform lock entries were regenerated by npm. Stale nested esbuild resolutions were removed before npm resolved the supported override; clean installation and `npm ls` confirm a valid tree.

The new dependency compatibility test is a direct `test:contracts` command. No test gate is skipped, schema file edited, provider enabled, or production service changed.

## Evidence

- Clean `npm ci --include=dev` and `npm ls esbuild qs sharp --all` pass.
- `npm audit --json` reports zero advisories, down from 10 (8 moderate, 2 high). This is the registry's dependency advisory result, not a complete security audit.
- `node scripts/sway-security-dependencies.behavior.test.mjs` passes against the installed native decoder, query library, loader, and browser target resolver.
- Actual Drizzle generation against `src/db/schema.ts` succeeds with output in a disposable directory outside the source tree.
- The existing audio storage integration suite passes against its migrated disposable PGlite database. This verifies existing artwork and media behavior; it is not standalone PostgreSQL proof.
- Full lint and production build pass locally. Exact-candidate hosted contracts/native/browser acceptance must still pass before this follow-up receives release acceptance. Earlier recovery proof does not attest to the changed dependency tree.

Rollback restores the preceding package manifest/lock and removes the new compatibility gate only as part of reverting this follow-up. Production, provider transactions and live-money authorization remain outside this draft.
